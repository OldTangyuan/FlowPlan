# FlowPlan ProGuard 规则
#
# 当前 beta 版本未启用 R8 代码压缩（isMinifyEnabled = false），
# 此文件保留为空占位，未来如果启用 minify，在这里添加 keep 规则。
#
# Flutter 应用启用 minify 时的常见保留规则示例：
#   -keep class io.flutter.** { *; }
#   -keep class com.flowplan.flowplan.** { *; }
